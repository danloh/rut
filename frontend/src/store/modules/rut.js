// initial state
const state = {
  rutlist: [],
  rutdetali: {}
}

// getters
const getters = {
  rutlist: state => state.rutlist,
  rutdetail: state => state.rutdetail
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
